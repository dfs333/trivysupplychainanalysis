\* Refinement, VULNERABLE workflow: floating tags + valid stolen credential.
\* Expected: RefinesSecure FAILS -- TLC returns a behavior whose mapped `leaked`
\* flips false -> true (the residual attack surface).
SPECIFICATION Spec
CONSTANTS
    Victims = {v1, v2, v3, v4, v5}
    Repos = {trivy_action, setup_trivy}
    Tags = {tg1, tg2, tg3, tg4, tg5}
    Commits = {c_benign, c_malicious, c_other}
    BenignCommit = c_benign
    MaliciousCommit = c_malicious
    AquaServiceAcct = AquaServiceAcct
    ShaPinnedVictims = {}
    ServiceAcctRotated = FALSE
INVARIANT TypeOK
PROPERTY RefinesSecure
