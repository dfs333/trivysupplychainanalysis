\* Row 3 of the validation table: "SHA pins + valid stolen cred".
\* Expected: NoExfiltration HOLDS.  Universal SHA pinning protects victims even
\* though the residual credential is still valid: the attacker can still mutate
\* tags, but no victim resolves the action through a tag, so the malicious
\* commit never executes.  Residual access vulnerability NOT triggered.
SPECIFICATION Spec
CONSTANTS
    Victims = {v1, v2, v3, v4, v5}
    Repos = {trivy_action, setup_trivy}
    Tags = {tg1, tg2, tg3, tg4, tg5}
    Commits = {c_benign, c_malicious, c_other}
    BenignCommit = c_benign
    MaliciousCommit = c_malicious
    AquaServiceAcct = AquaServiceAcct
    ShaPinnedVictims = {v1, v2, v3, v4, v5}
    ServiceAcctRotated = FALSE
INVARIANT TypeOK
INVARIANT NoExfiltration
